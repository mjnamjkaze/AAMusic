<?xml version="1.0" encoding="utf-8"?>
<!-- Một bài trong bảng thư viện. Cao 72dp: chạm trúng được khi xe đang chạy. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:minHeight="72dp"
    android:orientation="horizontal"
    android:paddingStart="16dp"
    android:paddingTop="6dp"
    android:paddingEnd="8dp"
    android:paddingBottom="6dp">

    <com.google.android.material.card.MaterialCardView
        android:layout_width="72dp"
        android:layout_height="46dp"
        app:cardBackgroundColor="@color/drive_surface"
        app:cardCornerRadius="8dp"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/trackThumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:padding="12dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_music_note"
            app:tint="@color/drive_text_dim" />
    </com.google.android.material.card.MaterialCardView>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/trackTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/drive_text"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/trackSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/drive_text_dim"
            android:textSize="13sp" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/trackQueue"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/drive_add_to_queue"
        android:padding="14dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_queue_add"
        app:tint="@color/drive_text_dim" />

    <ImageButton
        android:id="@+id/trackRemove"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/search_remove_one"
        android:padding="15dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_close_small"
        app:tint="@color/drive_text_dim" />

</LinearLayout>
